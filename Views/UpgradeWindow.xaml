<Window x:Class="ChatRWKV_PC.Views.UpgradeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:sy="http://schemas.syncfusion.com/wpf"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatRWKV_PC"
        mc:Ignorable="d"
        Title="更新依赖包" Height="120" Width="200" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        <ComboBox x:Name="AllVersionComboBox" SelectedIndex="0" SelectionChanged="AllVersionComboBox_SelectionChanged" Margin="5"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Content="确认" Width="85" Grid.Column="0" x:Name="ConfirmBtn" Click="ConfirmBtn_Click" />
            <Button Content="取消" Width="85"  Grid.Column="1" x:Name="CancelBtn" Click="CancelBtn_Click"/>
        </Grid>
    </Grid>
</Window>
